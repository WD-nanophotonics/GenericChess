# ADR-119 H49C closeout addendum: F49 architectural transport blocker

Status: Accepted F49 closeout

Parent sandbox checkpoint: `287962de6d54060d4c65f680e8d812646119f8a9`

## Decision

Close F49 as `BLOCKED_ARCHITECTURAL_TRANSPORT`. The causal finding is
`CANONICAL_SEMANTIC_RULESET_NOT_REPRESENTABLE_BY_CURRENT_LEGACY_NATIVE_SEARCH_TRANSPORT`.
F49's scientific hypotheses are `NOT_MEASURED_DUE_TO_PREREQUISITE_FAILURE`;
no evaluator, learning, teacher, corpus, or Python-vs-Native conclusion may be
inferred from the aborted attempts.

## Evidence preserved

R5 certified the initial action boundary as Western 20 semantic / 4 legacy /
16 missing, including the required `e2-e4`; Standard Shogi is 30/30; the
selected generated RuleSet is 14/14. The R3 and R4 roots remain permanently
quarantined with evidentiary status `NONE`. No valid F49 Native or Python
search matrix exists, and no selector A/B/C/D/E classification is inferred.
The complete machine-readable record is
`tests/fixtures/h49c_f49_architectural_blocker_closeout.json`.

Production diff is zero. S49 was not regenerated, Native/Python F49 search and
learner execution were not invoked, and F50 is selected but remains
`NOT_STARTED`.

## Next architectural boundary

The next boundary is `F50_SEMANTIC_NATIVE_SEARCH_EXECUTION_PATH`. Planning must
compare reuse or extension of existing Native semantic IR execution, a
semantic-native search representation preserving public action identity and
auxiliary state, and a Python semantic material-injection control path. The
Python route is not a default workaround; the target remains a generic search
path that represents the canonical semantic RuleSet.
